<div class="container" style="margin-top: 100px">
  <div class="card">
    <div class="card-header bg-danger text-white">
      <strong><%= @current_provider.first_name %>, edit your profile.</strong>
    </div>



  <div class="card-body">

    <div class="container">
<div class="row row-cols-md-5" style="margin-top: 25px">

  <div class="cols mb-4 border-0">
    <div class="card border-0 bg-light">
      <div class="card-header bg-dark text-white border-0">
        Add languages
      </div>

      <div class="card-body">
        <% Language.all.each do |a_language|%>
          <form action="/insert_spoken_language" method="post">
            <label for="language_<%= a_language.id %>"></label>
            <input id="language_<%= a_language.id %>" type="hidden" name="query_language_id" value="<%= a_language.id %>">

            <button type="submit" class="btn btn-dark" style="margin-top: 10px"><%= a_language.name %></button>
          </form>
        <% end %>
      </div>
    </div>

    <div class="card-header bg-danger text-white border-0">
      Remove languages
    </div>

    <div class="card-body">
      <table>
        <% @the_provider.spoken_languages.each do |a_spoken_language| %>

          <tr>
            <td>
              <%= a_spoken_language.language.name %>
            </td>

            <td>
              <a href="/delete_spoken_language/<%= a_spoken_language.id %>">
                <button class="btn btn-danger">
                  Remove
                </button>
              </a>
            </td>
          <tr>

        <% end %>
      </table>
    </div>
  </div>

  <div class="cols mb-4 border-0">
    <div class="card border-0 bg-light">
      <div class="card-header bg-dark text-white border-0">
        Add neighborhoods
      </div>

      <div class="card-body">
        <% Neighborhood.all.each do |a_neighborhood|%>
          <form action="/insert_area" method="post">
            <label for="neighborhood_<%= a_neighborhood.id %>"></label>
            <input id="neighborhood_<%= a_neighborhood.id %>" type="hidden" name="query_neighborhood_id" value="<%= a_neighborhood.id %>">

            <button type="submit" class="btn btn-dark" style="margin-top: 10px"><%= a_neighborhood.name %></button>
          </form>
        <% end %>
      </div>
    </div>

    <div class="card-header bg-danger text-white border-0">
      Remove neighborhoods
    </div>

    <div class="card-body">
      <table>
        <% @the_provider.areas.each do |a_area| %>

          <tr>
            <td>
              <%= a_area.neighborhood.name %>
            </td>

            <td>
              <a href="/delete_area/<%= a_area.id %>">
                <button class="btn btn-danger">
                  Remove
                </button>
              </a>
            </td>
          <tr>

        <% end %>
      </table>
    </div>
  </div>

  <div class="cols mb-4 border-0">
    <div class="card border-0 bg-light">
      <div class="card-header bg-dark text-white border-0">
        Add special needs
      </div>

      <div class="card-body">
        <% Need.all.each do |a_need|%>
          <form action="/insert_service" method="post">
            <label for="need_<%= a_need.id %>"></label>
            <input id="need_<%= a_need.id %>" type="hidden" name="query_need_id" value="<%= a_need.id %>">

            <button type="submit" class="btn btn-dark" style="margin-top: 10px"><%= a_need.name %></button>
          </form>
        <% end %>
      </div>
    </div>

    <div class="card-header bg-danger text-white border-0">
      Remove special needs
    </div>

    <div class="card-body">
      <table>
        <% @the_provider.services.each do |a_service| %>

          <tr>
            <td>
              <%= a_service.need.name %>
            </td>

            <td>
              <a href="/delete_service/<%= a_service.id %>">
                <button class="btn btn-danger">
                  Remove
                </button>
              </a>
            </td>
          <tr>

        <% end %>
      </table>
    </div>
  </div>

  <div class="cols mb-4 border-0">
    <div class="card border-0 bg-light">
      <div class="card-header bg-dark text-white border-0">
        Add skills
      </div>

      <div class="card-body">
       <% Task.all.each do |a_task|%>
          <form action="/insert_skill" method="post">
            <label for="task_<%= a_task.id %>"></label>
            <input id="task_<%= a_task.id %>" type="hidden" name="query_task_id" value="<%= a_task.id %>">

            <button type="submit" class="btn btn-dark" style="margin-top: 10px"><%= a_task.name %></button>
          </form>
        <% end %>
      </div>
    </div>

    <div class="card-header bg-danger text-white border-0">
      Remove skills
    </div>

    <div class="card-body">
      <table>
        <% @the_provider.skills.each do |a_skill| %>

          <tr>
            <td>
              <%= a_skill.task.name %>
            </td>

            <td>
              <a href="/delete_skill/<%= a_skill.id %>">
                <button class="btn btn-danger">
                  Remove
                </button>
              </a>
            </td>
          <tr>

        <% end %>
      </table>
    </div>
  </div>

  <div class="cols mb-4 border-0">
    <div class="card border-0 bg-light">
      <div class="card-header bg-dark text-white border-0">
        Add payments
      </div>

      <div class="card-body">
       <% PaymentType.all.each do |a_payment|%>
          <form action="/insert_payment_accepted" method="post">
            <label for="payment_<%= a_payment.id %>"></label>
            <input id="payment_<%= a_payment.id %>" type="hidden" name="query_payment_type_id" value="<%= a_payment.id %>">

            <button type="submit" class="btn btn-dark" style="margin-top: 10px"><%= a_payment.name %></button>
          </form>
        <% end %>
      </div>
    </div>

    <div class="card-header bg-danger text-white border-0">
      Remove payments
    </div>

    <div class="card-body">
      <table>
        <% @the_provider.payment_accepteds.each do |a_payment_acc| %>

          <tr>
            <td>
              <%= a_payment_acc.payment_type.name %>
            </td>

            <td>
              <a href="/delete_payment_accepted/<%= a_payment_acc.id %>">
                <button class="btn btn-danger">
                  Remove
                </button>
              </a>
            </td>
          <tr>

        <% end %>
      </table>
    </div>
  </div>
</div>

<div class="d-flex">
  <form action="/provider/<%= @current_provider.id %>">
    <button type="submit" class="btn btn-danger" style="padding: 10px 30px 10px 30px; margin-bottom: 50px"><strong>Update Information</strong></button>
  </form>
</div>


<hr>




    <form action="/modify_provider" method="post" enctype="multipart/form-data">
      
      <div class="form-group">
        <img src="<%= @current_provider.image %>" class="img-responsive" width="250px">
        <input class="form-control" type="file" id="image_box" name="image" class="form-control">
      </div>

      <div class="form-group">
        <label for="first_name_box">First name</label>
        <input class="form-control" id="first_name_box" name="query_first_name" value="<%= @current_provider.first_name %>">
      </div>

      <div class="form-group">
        <label for="last_name_box">Last name</label>
        <input class="form-control" id="last_name_box" name="query_last_name" value="<%= @current_provider.last_name %>">
      </div>

      <div class="form-group">
        <label for="email_box">Email</label>
        <input class="form-control" id="email_box" name="query_email" value="<%= @current_provider.email %>">
      </div>

      <div class="form-group">
        <label for="price_box">Price/hr</label>
        <input class="form-control" id="price_box" name="query_price" value="<%= @current_provider.price.to_i %>">
      </div>

      <div class="form-group">
        <label for="description_box">Description</label>
        <textarea class="form-control" id="description_box" name="query_description" value="<%= @current_provider.description %>"></textarea>
      </div>

      <div class="form-group">
        <label for="password_box">Password</label>
        <input class="form-control" id="password_box" name="query_password" placeholder="Choose a password..." type="password">
      </div>
    
      <div class="form-group">
        <label for="password_confirmation_box">Password Confirmation</label>
        <input class="form-control" id="password_confirmation_box" name="query_password_confirmation" placeholder="Confirm your password..." type="password">
      </div>

      <button type="submit" class="btn btn-danger">
        <strong>Update Account</strong>
      </button>
    </form>
  </div>
</div>
</div>

